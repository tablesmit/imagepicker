= Page for test Image Picker = 


== Images from URLs without extension test ==

Images from URLs without extensions are saved without extensions. You should use the MIME service to ensure that the URL's current extension is appropriate to the image's MIME type.